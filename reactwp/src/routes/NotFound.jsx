import React from 'react';
import { useRouteError } from 'react-router-dom';

const NotFound = () => {
    return(
        <div>
            NOT FOUND
        </div>
    )
}

export default NotFound;
